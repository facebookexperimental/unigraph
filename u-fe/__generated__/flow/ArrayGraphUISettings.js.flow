/** 
 * Copyright (c) Meta Platforms, Inc. and affiliates.
 *
 * @flow
 * @generated
 */


import type { ArrayGraphUISettingsTreeTableEntryPoints } from './ArrayGraphUISettingsTreeTableEntryPoints.js';
import type { ColumnSettings } from './ColumnSettings.js';
import type { GraphStructure } from './GraphStructure.js';
import type { GraphTableSort } from './GraphTableSort.js';
import type { SidebarPanel } from './SidebarPanel.js';

export type ArrayGraphUISettings = {
  selected_sidebar_panel?: ?SidebarPanel,
  graph_table_sort?: ?GraphTableSort,
  columns?: ?ColumnSettings,
  graph_structure?: ?GraphStructure,
  entry_points?: ?ArrayGraphUISettingsTreeTableEntryPoints,
  // Used in combination with `entry_points` settings.` If entry_points is set to `Specified`, this value will be used to determine entry points. This value is stored separately so we can preserve selected entry points when switching between different entry points settings. E.g. if we're exploring `reverse from a specific node` and want to hop into `show as flat list`, we want to preserve the selected entry points, so when we switch back to "reverse" we keep the same selected entry point
  entry_points_specified?: ?Array<string>,
};