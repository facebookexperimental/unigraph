[package]
edition = "2024"
name = "unigraph_git"
publish = false
version = "0.0.0"

[dependencies]
anyhow = "1"
chrono = { version = "0.4", features = ["clock", "serde", "std"], default-features = false }
git2 = "0.20"
