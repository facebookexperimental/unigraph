# @generated by autocargo from //unigraph/oss:typegen_test

[package]
name = "typegen_test"
version = "0.1.0"
authors = ["Meta Platforms, Inc. and affiliates"]
edition = "2024"
license = "MIT"

[dependencies]
typegen = { path = "../typegen" }

[dev-dependencies]
k9 = { version = "0.12.0", features = ["custom_comparison_formatters", "regex"] }
