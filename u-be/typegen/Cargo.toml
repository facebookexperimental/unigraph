# @generated by autocargo from //unigraph/oss:typegen

[package]
name = "typegen"
version = "0.1.1-alpha.0"
authors = ["Meta Platforms, Inc. and affiliates"]
edition = "2024"
description = "Generate TypeScript, Flow, and Hack types from Rust struct definitions"
homepage = "https://github.com/facebookexperimental/unigraph"
repository = "https://github.com/facebookexperimental/unigraph"
license = "MIT"

[dependencies]
anyhow = "1.0.103"
md-5 = "0.10"
serde = { version = "1.0.219", features = ["derive", "rc"] }
toml = { version = "1.1.2", features = ["preserve_order"] }
typegen_derive = { version = "0.1.1-alpha.0", path = "../typegen_derive" }

[dev-dependencies]
k9 = { version = "0.12.0", features = ["custom_comparison_formatters", "regex"] }
