# @generated by autocargo from //unigraph/oss:unigraph_core

[package]
name = "unigraph_core"
version = "0.1.1-alpha.0"
authors = ["Meta Platforms, Inc. and affiliates"]
edition = "2024"
description = "Core graph data model: nodes, edges, metrics, tags, and traversal algorithms"
homepage = "https://github.com/facebookexperimental/unigraph"
repository = "https://github.com/facebookexperimental/unigraph"
license = "MIT"

[dependencies]
anyhow = "1.0.103"
bitflags = { version = "2.11.1", features = ["serde", "std"], default-features = false }
bytemuck = { version = "1.25", features = ["const_zeroed", "derive", "min_const_generics", "must_cast", "nightly_portable_simd", "nightly_stdsimd"] }
fst = "0.4"
ll = "10.0.0"
rayon = "1.11.0"
readonly = "0.2.13"
regex = "1.12.4"
serde = { version = "1.0.219", features = ["derive", "rc"] }
serde_json = { version = "1.0.140", features = ["alloc", "float_roundtrip", "raw_value", "unbounded_depth"] }
typegen = { version = "0.1.1-alpha.0", path = "../typegen" }
unigraph_delta = { version = "0.1.1-alpha.0", path = "../unigraph_delta" }
unigraph_serialization = { version = "0.1.1-alpha.0", path = "../unigraph_serialization" }
unigraph_timestamp = { version = "0.1.1-alpha.0", path = "../unigraph_timestamp" }
xxhash-rust = { version = "0.8.15", features = ["xxh3", "xxh64"] }

[dev-dependencies]
k9 = { version = "0.12.0", features = ["custom_comparison_formatters", "regex"] }
maplit = "1.0"
